[workspace]
channels = ["conda-forge"]
platforms = ["osx-arm64", "linux-64"]

[dependencies]
python = ">=3.10"
cartopy = "*"
geopandas = "*"
geopy = "*"
h5py = "*"
matplotlib = "*"
networkx = "*"
numpy = ">=1.19.5"
pandas = "*"
paramiko = "*"
psycopg = "*"
scikit-learn = ">=1.1"
scipy = "*"
tqdm = "*"
pytest = "*"
pytest-cov = "*"
ruff = "*"
xmltodict = "*"
scikit-image = "*"
shapely = "*"
obspy = "*"
pip = "*"

[feature.dev.dependencies]
# Type stubs for better IDE support and type checking
types-tqdm = "*"
types-requests = "*"
types-paramiko = "*"
# psycopg (v3) ships with inline type annotations (py.typed), no stub needed
types-setuptools = "*"
postgresql = ">=18.4,<19"

[feature.dev.pypi-dependencies]
# Type checker (using ty by Astral - makers of ruff and uv)
ty = "*"

# Git hooks framework for running checks before commits
pre-commit = "*"

# Better commit messages (conventional commits)
commitizen = "*"

[pypi-dependencies]
geode-gnss = { path = ".", editable = true }
country-converter = "*"
dirsync = "*"
dispy = "*"
earthscope-cli = "*"
earthscope-sdk = "*"
hdf5storage = "*"
munch = "*"
pycos = "*"
simplekml = "*"
usgs-libcomcat = "*"
legacy-cgi = ">=2.6.4, <3"
python-dotenv = ">=1.0.0"

[feature.reports.pypi-dependencies]
staticmap = ">=0.5.7"
weasyprint = ">=60.0"

[environments]
# Default environment includes dev features for local development
default = { features = ["dev"], solve-group = "default" }

# Docs environment for building documentation
docs = { features = ["docs"], solve-group = "default" }
reports = { features = ["dev", "reports"], solve-group = "default" }
web = { features = ["dev", "web"], solve-group = "default" }

[feature.docs.pypi-dependencies]
mkdocs = "*"
mkdocs-material = "*"
pymdown-extensions = "*"

[tasks]
# Testing
test = { cmd = "pytest" }
"test:api" = { cmd = "pytest geode/tests/test_antenna_api.py" }
"test:verbose" = { cmd = "pytest -v" }
"test:cov" = { cmd = "pytest --cov=geode --cov-report=term-missing" }

# Linting and formatting
lint = { cmd = "ruff check geode com" }
"lint:fix" = { cmd = "ruff check --fix geode com" }
format = { cmd = "ruff format geode com" }
"format:check" = { cmd = "ruff format --check geode com" }

# Type checking
typecheck = { cmd = "ty check --output-format github geode com" }

# Documentation
"docs:serve" = { cmd = "mkdocs serve" }
"docs:build" = { cmd = "mkdocs build" }

# Pre-commit hooks
"precommit:install" = { cmd = "pre-commit install --install-hooks" }
"precommit:run" = { cmd = "pre-commit run --all-files" }

# All checks (useful for CI)
check = { cmd = "ruff check geode com && ruff format --check geode com && ty check geode com && pytest" }
# Utilities
# check-external = "which gfzrnx && which crx2rnx && which gpspace && which sh_gamit"
# pgamit = "pgamit --help"
# plot = "PlotETM.py <sitecode>"

# Backend/API tests use the same Django and DRF series as the backend image.
[feature.web.pypi-dependencies]
django = "~=5.2.11"
djangorestframework = "~=3.15.2"
djangorestframework-simplejwt = "~=5.5.1"
django-auditlog = "==3.0.0"
django-filter = "==24.2"
django-cors-headers = "==4.3.1"
django-cleanup = "==8.1.0"
drf-spectacular = "==0.28.0"
drf-standardized-errors = "==0.13.0"
celery = "~=5.5.0"
redis = "~=5.2.0"
